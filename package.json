{
  "name": "wikipediapagepopularitymeter",
  "version": "1.0.0",
  "description": "See the relative popularity of pages on Wikipedia.",
  "scripts": {
    "start": "parcel watch src/manifest.json --host localhost --config @parcel/config-webextension",
    "build": "parcel build src/manifest.json --config @parcel/config-webextension"
  },
  "author": "Kyle Dagman",
  "license": "ISC",
  "devDependencies": {
    "@parcel/config-webextension": "^2.11.0",
    "parcel": "^2.11.0"
  }
}
